FUSEKI_SERVER_URL=http://spark-6d47:8001/
FUSEKI_DATASET=/idea_kg
PERSISTENT_URI_BASE=https://purl.org/twc/sudo/kg/
PUBLIC_BASE_PATH=/sudo-uri-resolver

# Conversation history notebook/component storage.
# Session IDs are generated as globally unique UUIDs. If a caller has a
# namespace, pass it inside metadata, not as part of identity or lookup.
CONVERSATION_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/paper_convo_buddy
CONVERSATION_ECHO_SQL=false
CONVERSATION_EMBEDDING_DIM=1536
